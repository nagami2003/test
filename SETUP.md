# 登録支援機関 Google Workspace 管理システム

特定技能外国人の支援業務・バックオフィスを Google Workspace で一元管理するシステムです。

## 機能一覧

| 機能 | 内容 |
|------|------|
| 外国人管理 | 支援対象者の情報・在留期限管理・ビザ期限アラート |
| 受入企業管理 | 契約企業の情報・契約期限管理 |
| 支援計画 | 特定技能12種類支援の実施状況管理・Google Doc自動生成 |
| 定期面談 | 3ヶ月毎の面談記録・Googleカレンダー自動登録・記録書自動生成 |
| 請求書管理 | Google Doc請求書自動生成・一括発行・未払い管理 |
| 売上管理 | 月次売上サマリー自動集計 |
| 自動通知 | ビザ期限・面談・請求書作成のメールアラート |
| Driveフォルダ | 外国人別・企業別フォルダ自動作成 |
| 法定書類 | 雇用条件書・事前ガイダンス確認書・支援委託契約書・生活オリエン確認書をDoc自動生成 |
| 多言語対応 | ビザ期限通知・生活案内を日/英/越/尼で本人へ送信 |
| 行政コンプラ | 出入国在留管理庁への四半期/随時届出の管理・チェックリスト生成 |
| 経営分析 | KPI集計・売上/国籍別/分野別グラフ・月次経営レポート |

---

## セットアップ手順

### 1. Google スプレッドシートを作成

1. Google ドライブで新規スプレッドシートを作成
2. 名前を「登録支援機関管理システム」等に変更

### 2. Apps Script にコードを貼り付け

**方法A: clasp（推奨 / ほぼ全自動）**

```bash
npm install            # clasp をローカルに導入
npm run login          # Googleアカウントでログイン（ブラウザが開きます）
npm run create         # スプレッドシート＋Apps Scriptを新規作成
npm run deploy         # 全コードをアップロード
npm run open           # 作成されたスプレッドシートを開く
```

`npm run create` 実行時に `.clasp.json` の `scriptId` が自動で書き換わります。
以降コードを変更したら `npm run deploy` だけで反映されます。

**方法A-2: GitHub Actions で自動デプロイ**

`main` ブランチの `src/` を変更すると自動デプロイされます。
GitHub の Settings → Secrets に次を登録してください:
- `CLASPRC_JSON`: ローカルで `npm run login` 後に生成される `~/.clasprc.json` の中身
- `SCRIPT_ID`: `npm run create` で発行されたスクリプトID

**方法B: 手動コピー**

1. スプレッドシートで「拡張機能」→「Apps Script」を開く
2. `src/` 内の各 `.gs` ファイルの内容を新規スクリプトとして追加
3. ファイル名は `.gs` 拡張子なしで設定（例: `Config`, `Menu`, `Setup`）

### 3. Config.gs を自社情報に変更

```javascript
COMPANY: {
  NAME: '株式会社〇〇登録支援機関',  // ← 自社名
  ADDRESS: '東京都〇〇区...',         // ← 住所
  PHONE: '03-0000-0000',             // ← 電話番号
  EMAIL: 'support@example.com',      // ← メールアドレス
  REGISTRATION_NUMBER: '登録番号 第〇〇〇〇〇〇〇〇〇号', // ← 登録番号
  BANK_NAME: '〇〇銀行 〇〇支店',    // ← 振込先銀行
  ACCOUNT_NUMBER: '0000000',         // ← 口座番号
  ACCOUNT_HOLDER: '株式会社〇〇',    // ← 口座名義
},
```

### 4. 初期セットアップを実行

スプレッドシートを開くとメニューに「🏢 支援機関システム」が表示されます。

1. **初期設定 → Driveフォルダ構造を作成**
2. **初期設定 → シートを初期化**
3. **通知・自動化 → 定期通知トリガーを設定**

### 5.（任意）動作確認：サンプルデータ

仕組みをすぐ確認したい場合、Apps Script エディタで `loadSampleData` を実行すると
企業2社・外国人3名・面談・請求書のサンプルが入り、ダッシュボードにアラートが表示されます。
本番運用前に `clearSampleData` で削除できます。

---

## 日常業務フロー

### 外国人受入時
1. 「外国人管理 → 外国人を新規登録」でフォームに入力
2. 「支援計画 → 支援計画書を作成」で Google Doc が自動生成
3. カレンダーに四半期面談が自動登録される

### 毎月
- 「請求・売上 → 一括請求書作成」で全社の請求書を一括生成
- Google Drive の請求書フォルダに自動保存

### 3ヶ月毎（定期面談）
1. カレンダーのリマインダーを確認
2. 「定期面談 → 面談を記録」でフォームに入力
3. 面談記録 Google Doc が自動生成

### 四半期毎（行政報告）
- 出入国在留管理庁への定期報告書を提出
- 自動リマインダーメールが1月・4月・7月・10月に送信される

---

## ファイル構成

```
src/
├── appsscript.json   # Apps Scriptマニフェスト
├── Config.gs         # 設定（自社情報・定数）← 必ず編集
├── Menu.gs           # カスタムメニュー
├── Setup.gs          # 初期設定・ダッシュボード
├── ForeignWorkers.gs # 外国人管理
├── Companies.gs      # 受入企業管理
├── SupportPlans.gs   # 支援計画管理
├── Interviews.gs     # 定期面談管理
├── Invoices.gs       # 請求書管理
├── Notifications.gs  # 自動通知・トリガー
├── Documents.gs      # 法定書類のGoogle Doc自動生成
├── I18n.gs           # 多言語対応（日/英/越/尼）
├── Compliance.gs     # 行政コンプライアンス・届出管理
├── Analytics.gs      # 経営分析・KPI・月次レポート
└── SampleData.gs     # 動作確認用サンプルデータ（任意）
```

## 必要な権限（OAuthスコープ）

初回実行時に Google から権限承認を求められます（`appsscript.json` で定義）:
スプレッドシート / Drive / Gmail送信 / カレンダー / ドキュメント

すべて自社の Google Workspace アカウント内で完結し、外部送信はありません。
